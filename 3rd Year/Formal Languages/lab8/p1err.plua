-- Invalid reserved word 'fun'
fun max(a, b, c) {
    local max = a
    if b > max {
        max = b
    }
    if c > max {
        max = c
    }
    return max
}

-- Invalid symbol '$' in constant.
print(max(1, 3$4, 2))